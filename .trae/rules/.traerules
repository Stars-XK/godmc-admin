# Project Rules

## 极速批量导入规范
在所有的 Excel 数据导入场景中，严禁使用传统的 `el-upload` 直接将文件上传至后端解析。必须严格遵循“站点、设备、测点”的极速导入架构：
1. **前端本地解析**：在浏览器端使用 `xlsx` 库将选中的 Excel 文件转为 JSON 数组。
2. **沉浸式全屏交互**：文件选中后，导入弹窗瞬间关闭，并弹出覆盖全屏的 `backdrop-filter: blur` 毛玻璃进度条（带有雷达脉冲圈动画），展示解析、处理的实时进度。
3. **前端分批推送**：由于请求体限制，前端必须将解析出的大数组切片（例如 `500条/批`），通过 `for` 循环加上 `await` 逐批推送到后端的 `@Post('importBatch')` 接口。同时需配置超长超时时间（如 `timeout: 600000`）。
4. **后端分批落库**：后端在接收到 JSON 数组后，同样采用切片分批（例如 `500条/批`）调用 `save()` 方法插入数据库，防止单条 SQL 过长引发 `max_allowed_packet` 异常或 Node.js OOM。

## 列表页规范
列表页默认必须为 20 行（`pageSize: 20`），并且表格外层容器必须使用 Flex 布局，确保在数据不够时 `el-table` 也能向下撑满整个屏幕高度。

## Git Workflow
每次处理完任务需要提交所有内容到github，并且push到主分支。

操作步骤示例：
```bash
git add .
git commit -m "feat/fix: update according to task"
git push origin main
```
在完成任何任务的最后一步，都必须执行上述操作。
